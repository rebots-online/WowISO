{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: invoke commands + listen to backend events.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
